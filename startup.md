I keep having trouble sorting out the .venv and actually getting flask to run

.venv/Scripts/activate needs to be done when in the WEBDEV directory
pip install flask 
pip install Flask-Login
pip install openai     
pip install SQLAlchemy 